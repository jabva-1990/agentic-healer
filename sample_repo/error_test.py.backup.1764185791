#!/usr/bin/env python3
"""
New test file with various Python errors
"""

import os
import json
import requests  # This might not be installed

class DataProcessor:
    """Class with syntax error - missing colon"""
    
    def __init__(self):
        self.data = []
        self.results = None
    
    def process_items(self, items):
        """Process items with various issues"""
        for item in items:
            if item > 0:
                # Missing colon in if statement
                self.data.append(item * 2)
            else:
                self.data.append(0)
        
        return self.data
    
    def save_results(self, filename):
        """Save results to file with error handling issues"""
        # No error handling for file operations
        with open(filename, 'w') as f:
            json.dump(self.data, f)
        # Missing closing parenthesis
        
def calculate_average(numbers):
    """Calculate average with potential division by zero"""
    return sum(numbers) / len(numbers)  # No check for empty list

def main():
    processor = DataProcessor()
    test_data = [1, 2, -1, 4, 0]
    
    results = processor.process_items(test_data)
    avg = calculate_average(results)
    
    print(f"Average: {avg}")
    processor.save_results("output.json")

if __name__ == "__main__":
    main()