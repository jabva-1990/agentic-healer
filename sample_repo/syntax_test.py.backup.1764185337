#!/usr/bin/env python3
"""
Simple test file with obvious syntax error for self-healing agent testing.
"""

import os
import sys
import logging

# Configure basic logging to ensure info messages are displayed
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')

# non_existent_module # Import error - Removed as it causes ModuleNotFoundError

def hello_world():
    """Simple function with syntax error."""
    logging.info("Hello World from hello_world function") # Indentation and placement corrected
    return "success"

def calculate_sum(a, b): # Missing closing paren and colon added
    """Function missing colon and closing paren."""
    return a + b

def main():
    """Main function."""
    result = hello_world()
    sum_result = calculate_sum(5, 10)
    logging.info(f"Result: {result}")
    logging.info(f"Sum: {sum_result}") # Changed from print to logging.info for consistency

if __name__ == "__main__":
    main()